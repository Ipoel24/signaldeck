# Required at runtime for database operations (not required to build the app)
DATABASE_URL=postgresql://signaldeck:signaldeck@localhost:5432/signaldeck

# Public app origin
NEXT_PUBLIC_APP_URL=http://localhost:3000
